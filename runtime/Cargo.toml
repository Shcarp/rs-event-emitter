[package]
name = "runtime"
version = "0.1.0"
edition = "2021"

[dependencies]
futures = "0.3.30"
tokio = { version = "1.39.3", features = ["full"], optional = true }

[features]
tokio_runtime = ["dep:tokio"]

